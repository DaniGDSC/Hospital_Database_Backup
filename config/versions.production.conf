# Production version overrides — LOCKED
# NEVER change without: dev+staging test, Senior DBA approval,
# maintenance window, rollback plan ready
SQLSERVER_EDITION="Standard"
ENVIRONMENT_TIER="production"
VERSION_CHECK_STRICT="true"
