# Development version overrides
# Dev may use newer versions for testing — not locked
SQLSERVER_EDITION="Developer"
AWSCLI_PINNED_VERSION=""  # Dev allows any version >= min
ENVIRONMENT_TIER="development"
VERSION_CHECK_STRICT="false"
